# Clean-RAG-Application-Flask
This project builds a RAG application, where search details form uploaded documents, and cites the found information from uploaded resources, not hallucinating. It uses local LLM models via Ollama. Created a web-based UI using Flask to interact with the model.
